# Development dependencies — test runner, fixtures, coverage.
# Install with:
#   .venv/bin/pip install -r requirements-dev.txt
pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
httpx>=0.27          # FastAPI TestClient

{
  "name": "scribe-tests",
  "private": true,
  "type": "module",
  "description": "JS test harness for Scribe (Vitest + jsdom). Application code is in scribe/static/js/ and inline in scribe/templates/*.html.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^2.1.0",
    "jsdom": "^25.0.0",
    "vitest": "^2.1.0"
  }
}
